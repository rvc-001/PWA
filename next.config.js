/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optional: enable static export for minimal server footprint
  // output: 'export',
};

module.exports = nextConfig;
